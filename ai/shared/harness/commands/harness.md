---
description: /cosmos 의 옛 이름. 동일 파이프라인(Planner→Dev→QE→Ops).
argument-hint: <spec 경로 또는 러프한 요청>
---

`/harness`는 `/cosmos`로 이름을 바꿨습니다. `~/.claude/commands/cosmos.md`를 읽고 **그대로** 따르세요. 에이전트는 `cosmos-planner` / `cosmos-dev` / `cosmos-qe` / `cosmos-ops`입니다.
